Session start: re-read both changed instruction files (Usher CLAUDE.md, Arranger CLAUDE.md); ran
agentics upstream check; identified breaking changes and propagation items.

Agentics upstream items surfaced:
- sessions-per-contributor-file (breaking, 2026-07-08): Usher still on single sessions.md; applied this session
- tech-debt-fix-field (breaking, 2026-07-10): tech-debt skeleton now requires `fix:` field; no entries in Usher yet, noted for future
- fix-orphaned-references (breaking, 2026-07-10): template dispatch table gaps; addressed by adding Workflow section to AGENTS.md
- writing-for-a-cold-reader (non-breaking, 2026-07-10): added to documentation.md in agentics; relevant to current documentation work

Created `/docs/intro.md`: consumer-facing introduction to Usher; written for two audiences (data/bioinformatics users and developers); problem-first structure with natural escalation to technical detail; IAM concepts kept surface-level with a link to the forthcoming IAM Primer; GitHub URLs used for `.dev/` cross-links per agentics documentation convention.

- `docs/` directory created as the start of the consumer-facing docs tier
- `docs/intro.md` (rewritten): Usher not named until the "How Usher implements these patterns" section; four generic solution patterns described before the product (decision/enforcement separation, explicit grants, secure token delivery, revocation); GitHub URLs for all `.dev/` cross-links per agentics convention
- `.dev/design/README.md` reading-order and coverage table will need updating to reflect the new `/docs/` tier (logged below)

Relocated `docs/concepts.md` (moved from `.dev/design/concepts.md`):
- Internal links within concepts.md updated to GitHub URLs (permissions-model.md, admin-model.md) per agentics convention for /docs/-to-.dev/ cross-links
- All back-references in `.dev/design/` updated to `../../docs/concepts.md` (glossary.md, security-workflow.md, admin-model.md, README.md, permissions-model.md)
- `CLAUDE.md`, `AGENTS.md`: path mention updated to `docs/concepts.md`
- `docs/intro.md`: concepts.md added to "Where to go next" as a relative link

Migrated sessions format and synced instruction files with agentics conventions:
- `.dev/sessions.md` split into `.dev/sessions/`, one file per date (`YYYY-MM-DDTHHMMSS.md`); six historical entries preserved verbatim; `sessions.md` deleted
- `CLAUDE.md`: session checklist steps updated to `.dev/sessions/` lookup pattern; `--` prose connectors replaced with `: `; "Keeping .dev/ current" updated from `sessions.md` to `sessions/`
- `AGENTS.md`: session checklist and working documents updated to `.dev/sessions/` format; `--` prose connectors replaced with `: `; session-end discipline updated; new "Workflow" section added with dispatch pointers for `conventions/documentation.md` and `conventions/upgrading-adoption.md` in the agentics template
- `.github/copilot-instructions.md`: key references updated to `.dev/sessions/`; `--` prose connector fixed

Added inline anchor links from `docs/intro.md` to `docs/concepts.md` for first occurrences of defined terms: categories, Permissions, revocation, plugin, decision service, grant, resource, encrypted token, authorization, constraint token. Only the first occurrence of each term is linked; subsequent uses left as plain text.

Created `.dev/design/decisions.md`: tool evaluations (Cerbos, OPA at decision layer, OPA at enforcement layer, Cerbos Hub) with rationale for not adopting and explicit credit for design contributions; architectural decisions (constraint token, JWE, plugin enforcement, push revocation, fail-secure, data-agnostic). Updated `.dev/design/README.md`: added decisions.md to coverage table; replaced the brief "why we're building" paragraph with a summary and pointer to decisions.md.

Updated `.dev/design/README.md` reading order: added `docs/intro.md` as the first step for readers new to Usher; added "New to Usher entirely?" pointer in the intro paragraph.

Created `docs/why-usher.md`: layer model framing (authentication, coarse authz, fine-grained data access); Keycloak and OPA positioned as integration partners rather than alternatives; Cerbos, SpiceDB/Zanzibar, commercial SaaS, and per-app build-your-own as the genuine alternatives with honest assessment of each; "What Usher specifically adds" and "What Usher is not" closing sections. Linked from `docs/intro.md` "Where to go next".

Created `docs/iam-primer.md`: resolves dead link from `docs/intro.md`; covers the OAuth 2.0/OIDC ecosystem, Keycloak's role as IdP, JWT structure and standard claims, scopes, signed vs encrypted token distinction (pointer to concepts.md for depth), PDP/PEP/PAP brief introduction (pointer to concepts.md), and further reading with authoritative external links.

Added inline anchor links to `docs/iam-primer.md` for first occurrences of all terms defined in concepts.md: authorization, JWTs, authentication, JWS, constraint tokens, JWE, PDP, PEP, PAP, PIP, ABAC, permissions model, fail-secure, revocation. Same first-occurrence-only convention applied as in intro.md.

Added "Documentation" section to `.dev/roadmap.md` with two blocked items: `/docs/` integration guide (blocked on plugin-integration.md) and `/docs/` administration guide (blocked on admin model open questions and management UI design).

Rewrote `.dev/design/permissions-model.md` as a single restructured document.

Agreed on and applied a unified role vocabulary across all design and docs files. Renamed roles to drop redundant scope qualifiers and align with standard data governance terminology: `Platform admin` → `Admin`, `Category steward` → `Steward`, `Custodian` → `Owner`. Dropped `curator` entirely (legacy team naming with no external standard equivalent; context-specifically resolved to `owner` or `member`). The PAP admin architecture shorthand is retained in an explanatory note in admin-model.md and concepts.md but no longer used as a prose role name. Files updated: permissions-model.md, admin-model.md, glossary.md, management-ui.md, README.md, docs/concepts.md. Changes are unstaged in the working tree. The document had three readability problems: the entity schema table appeared before concepts were explained; "Open questions" contained resolved decisions inline; and the worked example appeared two sections after the visibility rule that it illustrates. New structure: design approach (model-agnostic language, hybrid RBAC+ABAC) → core concepts one by one (resources, memberships, categories, grants, groups) → entity schema → visibility rule + worked example + category composition together → category-to-field mapping → field-level restriction options → private data sharing use case → OCAP use case → category management (defaults, change propagation, embargo) → resolved decisions (deny by default, grant expiry path, include-list token, existence denial, custodianship at submission) → open questions (only the genuinely unresolved ones). No content was added or removed; the restructure is purely ordering and grouping.

Conducted an audience-perspective review of the consumer-facing docs (intro.md, why-usher.md, iam-primer.md, concepts.md) from three personas: PI evaluating adoption, researcher/data scientist, developer. Key gaps identified and logged in `.dev/roadmap.md`:

Design decisions needed (new "Researcher experience design" subsection):
- Access request workflow: how a researcher initiates a request for a category grant (no in-product flow designed yet)
- Denied access UX: what a member without a category grant sees (existence denial invariant vs. researcher confusion)
- Category grant expiry and renewal: notification mechanism and renewal path for researchers with time-limited access

Documentation gaps (expanded Documentation section):
- Available now: Steward role absent from docs/; GA4GH Passports needs optional framing; persona-differentiated navigation missing from intro.md; OCAP/data sovereignty invisible to docs-tier readers; `audience` concept only in glossary; why-usher.md tool comparison not suited as first read for non-technical audiences
- Blocked on design: researcher journey guide; denied access UX doc; expiry/renewal guide; constraint token payload schema

Full coherence and legibility pass across all docs/ files, roadmap, and tech-debt. Fixes applied:
- docs/concepts.md: "study/studies" replaced with "cohort" in three example sentences; Admin duplicate removed from PAP section (now defers to Privileged roles below); "Token TTL" renamed to "Constraint token TTL" with corresponding first-sentence fix; Controlled tier rewritten to give direct Admin/Steward path equal billing alongside DAC/GA4GH path; opening paragraph framing softened (was "each concept is threat-framed", which didn't hold for structural sections)
- docs/why-usher.md: "organisational" → "organizational"
- docs/iam-primer.md: "authorised" → "authorized"; link order corrected (intro.md before concepts.md); management UI marked as planned
- docs/intro.md: opening callout simplified (was two sentences duplicating the Where to go next navigation)
- roadmap.md: "sessions.md" reference updated to ".dev/sessions/"; "formalisation" → "formalization"; administration guide scope note added (data tier classification)

Resolved design decisions and roadmap cleanup:
- Multiple roles per user per resource: decided (supported; effective access is the union of all roles held). Documented as inline decision in permissions-model.md Memberships section; open question entry removed from permissions-model.md and roadmap.
- Technology stack roadmap item expanded: TypeScript confirmed; HTTP framework (Fastify/Hono, to confirm); policy store: PostgreSQL; shared cache and revocation pub/sub: Redis (complementary to PostgreSQL, not a replacement). Redis roles clarified: `generatedAt` fast-path cache across instances, and pub/sub backbone for push revocation channel.
- Roadmap: "Docs-tier gaps: available to action now" section removed (all items complete, logged in session history).

Technology stack decisions and roadmap updates:
- PostgreSQL (confirmed) + Valkey (confirmed; already deployed in Overture env; Redis-compatible). Each for its own purpose: PostgreSQL for the authoritative policy store; Valkey for shared constraint-payload cache and push revocation pub/sub backbone. Valkey Streams covers v1 durable in-process event needs.
- Fastify (strongly preferred, pending confirmation that no Usher-internal Express middleware needs reuse) + Pino (strongly preferred, native Fastify integration). Clarified that PEP plugins remain Express middleware regardless of Usher's framework choice; they communicate over HTTP.
- Kafka added to Future scope: upgrade path from Valkey Streams when external-consumer fan-out or event replay is required.
- Audit logging updated: dual-channel pattern documented (PostgreSQL for queryable source of truth; Pino log stream for real-time alerting and SIEM integration).
- Structured logging noted as v1 requirement from the start, not added later.

Addressed all six "available now" documentation gaps:
- docs/concepts.md: "Privileged roles" subsection added after User groups (covers Steward with OCAP explanation, and Admin); `audience` concept paragraph added to Constraint tokens section; GA4GH Passports section rewritten with explicit optional-path opener
- docs/intro.md: "Community data governance" paragraph added to How Usher implements these patterns; "Where to go next" restructured with persona-differentiated paths (PI/evaluator, researcher, developer, IAM newcomer) and note that any persona may become Steward or Admin
- docs/why-usher.md: opening callout extended with audience note directing non-technical readers to concepts.md
- roadmap.md: "available to action now" section updated to reflect all six items done

Created `.dev/design/architecture.md`: component responsibility map for all five components (Keycloak, controller, bridge, app plugin, host application) and two infrastructure services (PostgreSQL, Valkey); explicit "owns / does not own" boundaries per component; stateless host app principle stated; responsibility-at-a-glance table; Valkey's two distinct roles documented. Naming settled: controller (the Usher service / PDP+PAP), bridge / `usher-bridge` (the shared library embedded in host apps). Drawbridge metaphor documented in the bridge section: bridge raises on controller loss (fail-secure, 503), lowers on restored connectivity.

Added "Multi-instance propagation" subsection to `security-workflow.md` (after "Propagation: push + poll"): explains the Valkey pub/sub path that carries revocation events from the receiving instance to all others; numbered sequence covering both push-connected and poll-fallback paths; cross-reference to architecture.md.

Updated `.dev/design/README.md`: architecture.md added to the coverage table; reading order updated for "New to Usher" (architecture.md added after concepts.md) and "Planning a PEP plugin" (architecture.md added as the first step, replacing the glossary entry for integration concepts).

Naming finalized: controller (the Usher service) + bridge / `usher-bridge` (the embedded library). Propagated consistently across all docs: security-workflow.md (all plugin→bridge references for token exchange, caching, revocation channel, fail-secure, and open questions), glossary.md (usher-client entry renamed, drawbridge metaphor added), plugin-integration.md (usher-client→usher-bridge), README.md coverage table, roadmap.md (package name + revocation system bullet).

Roadmap: HTTP framework entry rewritten as a pre-implementation decision with Fastify and Hono both documented as genuine candidates, relative strengths noted for each; "strongly preferred" framing removed.

Design decision: JWE decryption is the bridge's responsibility, not the plugin's. Bridge holds the decryption key (provisioned at host app deploy time), decrypts the JWE, and exposes a typed `ConstraintPayload` to the plugin. Plugins never see the raw JWE or the key. Documented in architecture.md (bridge owns section, controller does-not-own, updated table rows), plugin-integration.md (new Decided section, Known requirements split into bridge vs plugin, decryption key distribution open question rewritten as bridge-scoped).

Consistency pass across all docs: every "does not own" item in architecture.md now names the component that does own it. PostgreSQL and Valkey boundary sections also updated to follow the same pattern.

Comprehensive grants-* rename across all docs (17 files): "constraint token" → "grants token", "permissions payload" → "grants payload", `PermissionsPayload` → `GrantsPayload`; broader positive-language pass ("Compute constraints" → "Compute grants", "translate constraints" → "translate grants payload", "constraint caching" → "grants caching", "grants computation engine" replacing "constraint computation engine", and equivalent changes in concepts.md, glossary.md, architecture.md, decisions.md, README.md, AGENTS.md, and copilot-instructions.md). Historical session files left unchanged.

Token structure diagram in security-workflow.md updated: replaced flat `resources`/`record_filters`/`exclude_categories` structure with the `grants` map (per-resource `role` + `categories` include-list); standard JWT claims added (`iss`, `aud`, `iat`).

"Grant computation pipeline" section added to security-workflow.md: three-tier additive model (open → registered → controlled), anonymous grants token path, benefits (uniform code path, universal audit log, consistent fail-secure); token structure reference with anonymous and authenticated examples.

"Additive grant pipeline with anonymous grants token for open data" decision added to decisions.md: rationale, three-tier pipeline, GA4GH/NIST standards consulted, tradeoffs accepted.

glossary.md "Open access" entry rewritten to match the new model: controller issues an anonymous grants token; bridge and plugin handle it identically to an authenticated one; plugin no longer derives an exclusion filter independently.

plugin-integration.md bridge requirements updated: anonymous token exchange (no bearer token required) documented as a bridge responsibility; grants payload schema open question updated to reference the decided `grants` map structure in security-workflow.md.

Broken anchor links fixed in docs/intro.md and docs/iam-primer.md (`#constraint-tokens` → `#grants-tokens` following concepts.md section rename).

Design review pass (peer review preparation): three documentation errors fixed inline (decisions.md plugin→bridge key; generatedAt ISO string→Unix integer across all examples; anonymous token missing iss); four pre-emptive notes added inline (public role synthetic at issuance, not stored as membership; bridge startup must establish revocation channel before serving requests; TTL default value rationale documented; service account CREATE_MEMBERSHIP indirect Owner escalation noted in admin-model.md). Created .dev/design/to-discuss.md: 20 design issues across token model, security model, permissions/roles, and operations requiring resolution before implementation; added to README.md coverage table.

Link audit: two broken relative paths fixed (docs/ → .dev/design/ must use GitHub URLs per agentics convention, not relative `../design/` paths): concepts.md grant-computation-pipeline link and why-usher.md decisions.md link both corrected. All anchor targets verified against actual headings in security-workflow.md.

Full consistency and redundancy pass across all 16 docs and supporting files (DEVELOPMENT.md, AGENTS.md, roadmap.md, admin-model.md included):

Terminology fixes:
- DEVELOPMENT.md: "constraint tokens" → "grants tokens" (missed in initial pass)
- why-usher.md: "Structured constraint output" → "Structured grants output"
- concepts.md: "a set of constraints" → "a structured set of grants" in Authorization definition; negative example rephrased to include-list form; JWE section "excluded from" → "holds grants for"; Grants tokens section negative example → positive framing
- security-threat-model.md: remaining "constraint token/tokens", "constraint data/values", "Constraint enforcement", "partial constraints", "their own constraints", "compute constraints" all updated to grants-* language
- roadmap.md: "Constraint computation engine" heading, "constraint extraction", "translating constraints" updated
- permissions-model.md: "constraint computation engine" → "grants computation engine"; `"resources"` key → `"grants"` in both worked-example JSON blocks
- permissions-model.md: "Attribute naming" open question updated: `grants` map key is settled, only field-level restriction names remain open; cross-reference to security-workflow.md added
- admin-model.md: "constraint computation is a mechanical derivation" → "grants computation"
- README.md: "Core constraint resolution" → "Core grants resolution"
- glossary.md: "Server-side filter" and "SQON" entries updated to remove `exclude_categories` (stale field name) and describe the correct include-list subtraction model

Inconsistency fixes:
- concepts.md "Data access tiers": Open tier description corrected from "Usher is not involved" to anonymous grants token model; table row updated; closing sentence updated; cross-reference to security-workflow.md grant computation pipeline added
- glossary.md "Open access": shortened to 3 sentences + link (was design rationale; glossary standard is 1-2 sentences + link)

Redundancy reductions:
- decisions.md: JWE, push revocation, and fail-secure sections each got a closing link to the relevant security-workflow.md section (canonical mechanism detail)
- why-usher.md: Cerbos paragraph now links to decisions.md for the full technical evaluation
- plugin-integration.md: Known requirements got a leading note pointing to architecture.md as the authoritative responsibility split
