Addressed UAC BRD review questions, designed the full ownership model, extended the threat model for insider threats, and created the audit events catalogue; two permissions model open questions resolved.

- `.dev/design/security-threat-model.md`: added PHR insider threat note to A01 (rogue or compromised steward; category-bounded scope, self-grant prohibition, bulk-operation alerting, unconditional admin revocation); extended A09 audit table with stewardship, ownership, admin override, and bulk grant events; added self-grant gap note
- `.dev/design/audit-events.md`: new file; catalogues all auditable events with required fields and severity level; scoped to the policy plane (Usher logs who holds what grants and when that changes; access decisions are the plugin layer's responsibility)
- `.dev/design/permissions-model.md`: added "Ownership cascade and invariants" section covering submitter as immutable audit property vs owner as a movable role, the four-step cascade, no-owner invariant and orphan handling, transfer-before-removal prerequisite, atomicity (plain-language definition included), config flag hierarchy, and unconditional admin override; resolved "Multi-category intersection" open question (per-resource scoping is sufficient for MVP; SQON-scoped grants is the post-MVP extension); resolved "Category grant scope" open question (always resource-specific)
- `.dev/design/decisions.md`: added FR-08 decision: study-level sharing satisfies snapshot sharing without snapshot machinery; SQON-scoped cohort sharing deferred to post-MVP
- `.dev/design/to-discuss.md`: added three items: self-grant prevention (HIGH; steward self-approval not yet blocked at PAP layer), visibility/embargo state overlap (MEDIUM; distinct governance reasons for the same technical effect must not be conflated), clinical-submission to Usher relationship (MEDIUM; pre-registration is the MVP approach; design needed before Lyric integration begins)
- `.dev/design/plugin-integration.md`: added logging requirements section; access-decision events (allow/deny) are the plugin's audit responsibility, not Usher's; required fields and cross-system correlation note added
- `.dev/roadmap.md`: added system context diagram (Design, first item), cohort registration and Lyric integration (Design), Usher ↔ portal-ui API contract (Design, last new item), SQON-scoped grants (Future scope); removed resolved items (category grant scope, multi-category intersection) from "Complete permissions model"
