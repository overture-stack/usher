Reconciled agentics adoption status against the current template (v0.9.0): the project's AGENTS.md/CLAUDE.md structure predates or diverged independently from the canonical dispatch-table shape, so no restructure or tag was applied; only two safe, style-matching additions were made.

- `AGENTS.md`: added two dispatch lines to the existing `## Workflow` section, matching its established "When X: read Y" prose pattern: softeng team member -> `CLAUDE.softeng.md`, Overture platform project -> `CLAUDE.overture.md`, both linked to the agentics template on GitHub (not copied in, per the "global-guideline material never lives in the project" rule)
- No other files changed: `CLAUDE.md` has no equivalent per-task dispatch pattern to extend cleanly (its `## Workflow` section is a single line pointing at global context), and the project's overall agentics-adoption status (drifted adopter vs. independently-converged vs. never-merged) could not be determined with confidence from the files alone, so no tag was added and no structural adoption (dispatch table, Interaction parameters, Critical constraints sections) was applied. Full findings reported to the developer for a decision.
- Developer confirmed full agentics adoption is wanted. Rebuilt `AGENTS.md` onto the canonical shape: Interaction parameters, Critical constraints (including this project's own extension of the instruction-file integrity check to also cover `.github/copilot-instructions.md`, a third file agentics' generic check doesn't know about), a "Project notes" section preserving every genuinely project-specific rule (design-first, model-agnostic, fail-secure, deny-by-default, the design-doc locations), the canonical dispatch table (folding in the two prose links added earlier this session plus the pre-existing "Security triggers" section as a project-specific addition on top of `security.md`), and the current 6-step Initialization flow. Tag added: `agentics-template-version: 0.9.0 | synced: b37cb60ff2668945f74347826ca6ccf24b5e80a9`. `CLAUDE.md` collapsed to the canonical stub, removing its independent, already-drifted copy of nearly everything `AGENTS.md` now holds. `.github/copilot-instructions.md` left untouched (out of scope, and editing it without explicit instruction is now one of `AGENTS.md`'s own Critical Constraints), but it was found to reference a stale path, `.dev/design/concepts.md`, that doesn't exist; the real file is `docs/concepts.md`. Flagged for the developer, not fixed.
